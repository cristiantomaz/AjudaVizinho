import { useEffect, useMemo, useState } from 'react'
import { login, logout, observeAuth, register } from './services/authService.js'
import { changeDonationStatus, createDonation as persistDonation, deleteDonation, listDonations, updateDonation } from './services/donationService.js'
import { createInterest, decideInterest, listInterests } from './services/interestService.js'
import { persistenceMode } from './services/firebase.js'

const categories = ['Todos', 'Roupas', 'Móveis', 'Livros', 'Brinquedos', 'Eletrônicos']
const statusLabels = { available: 'Disponível', reserved: 'Reservado', donated: 'Doado' }
const interestLabels = { pending: 'Aguardando', accepted: 'Aceito', rejected: 'Recusado' }
const iconByCategory = { Roupas: '👕', Móveis: '🪑', Livros: '📚', Brinquedos: '🧸', Eletrônicos: '🔌' }
const initialDonations = [
  { id: 1, title: 'Cadeira para escritório', category: 'Móveis', neighborhood: 'Vila Assunção', description: 'Cadeira em bom estado, com pequenos sinais de uso.', icon: '🪑', posted: 'Hoje', status: 'available' },
  { id: 2, title: 'Coleção de livros infantis', category: 'Livros', neighborhood: 'Centro', description: 'Seis livros ilustrados indicados para crianças de 6 a 9 anos.', icon: '📚', posted: 'Ontem', status: 'available' },
  { id: 3, title: 'Casaco de inverno', category: 'Roupas', neighborhood: 'Jardim Bela Vista', description: 'Casaco tamanho M, higienizado e pronto para uso.', icon: '🧥', posted: 'Há 2 dias', status: 'available' },
]

function Header({ currentUser, onAuth, onDashboard, onDonate, onLogout }) {
  return <header className="header"><a className="brand" href="#inicio"><span className="brand-mark">AV</span><span>AjudaVizinho</span></a><nav className="nav"><a href="#doacoes">Doações</a><a href="#como-funciona">Como funciona</a>{currentUser ? <div className="user-menu"><button className="link-button" onClick={onDashboard}>Minha conta</button><button className="link-button" onClick={onLogout}>Sair</button></div> : <button className="link-button" onClick={onAuth}>Entrar</button>}<button className="button button-small" onClick={onDonate}>Doar item</button></nav></header>
}

function DonationCard({ donation, onSelect }) {
  return <article className="donation-card"><div className={`card-visual visual-${String(donation.id).length % 4}`}><span>{donation.icon}</span><span className={`status status-${donation.status}`}>{statusLabels[donation.status] || 'Disponível'}</span></div><div className="card-content"><p className="eyebrow">{donation.category}</p><h3>{donation.title}</h3><p className="location">● {donation.neighborhood} · {donation.posted}</p><p>{donation.description}</p><button className="text-button" onClick={() => onSelect(donation)}>Ver detalhes →</button></div></article>
}

function DonationModal({ donation, currentUser, onClose, onEdit, onDelete, onInterest, onStatus }) {
  if (!donation) return null
  const isOwner = currentUser?.id === donation.ownerId
  return <div className="modal-backdrop" onMouseDown={onClose}><section className="modal" role="dialog" aria-modal="true" onMouseDown={(event) => event.stopPropagation()}><button className="close-button" onClick={onClose}>×</button><div className="modal-icon">{donation.icon}</div><p className="eyebrow">{donation.category} · {statusLabels[donation.status] || 'Disponível'}</p><h2>{donation.title}</h2><p className="location">● {donation.neighborhood} · {donation.posted}</p><p>{donation.description}</p>{isOwner ? <div className="owner-actions"><button className="button button-full" onClick={() => onEdit(donation)}>Editar publicação</button><label>Status<select value={donation.status} onChange={(event) => onStatus(donation, event.target.value)}>{Object.entries(statusLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><button className="danger-button" onClick={() => onDelete(donation)}>Excluir publicação</button></div> : <button className="button button-full" disabled={donation.status !== 'available'} onClick={() => onInterest(donation)}>{donation.status === 'available' ? 'Tenho interesse' : 'Item indisponível'}</button>}<small>O contato será combinado somente após a aprovação do doador.</small></section></div>
}

function DonationFormModal({ donation, onClose, onSave }) {
  const [form, setForm] = useState(donation ? { title: donation.title, category: donation.category, neighborhood: donation.neighborhood, description: donation.description } : { title: '', category: 'Roupas', neighborhood: '', description: '' })
  const [loading, setLoading] = useState(false)
  async function submit(event) { event.preventDefault(); setLoading(true); await onSave(form, donation); setLoading(false) }
  return <div className="modal-backdrop" onMouseDown={onClose}><section className="modal modal-form" role="dialog" aria-modal="true" onMouseDown={(event) => event.stopPropagation()}><button className="close-button" onClick={onClose}>×</button><p className="eyebrow">{donation ? 'Editar publicação' : 'Nova publicação'}</p><h2>{donation ? 'Atualize os dados da doação.' : 'Doe algo que já cumpriu seu papel na sua casa.'}</h2><form onSubmit={submit}><label>Nome do item<input required maxLength="60" value={form.title} onChange={(e) => setForm({ ...form, title: e.target.value })} /></label><div className="form-row"><label>Categoria<select value={form.category} onChange={(e) => setForm({ ...form, category: e.target.value })}>{categories.slice(1).map((item) => <option key={item}>{item}</option>)}</select></label><label>Bairro<input required maxLength="40" value={form.neighborhood} onChange={(e) => setForm({ ...form, neighborhood: e.target.value })} /></label></div><label>Descrição<textarea required maxLength="240" rows="4" value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} /></label><button className="button button-full" disabled={loading}>{loading ? 'Salvando…' : donation ? 'Salvar alterações' : 'Publicar doação'}</button></form></section></div>
}

function AuthModal({ onAuthenticated, onClose }) {
  const [mode, setMode] = useState('login'); const [form, setForm] = useState({ name: '', email: '', password: '', neighborhood: '' }); const [error, setError] = useState(''); const [loading, setLoading] = useState(false)
  async function submit(event) { event.preventDefault(); setError(''); setLoading(true); try { onAuthenticated(mode === 'register' ? await register(form) : await login(form)) } catch (caught) { setError(caught.message || 'Não foi possível concluir a autenticação.') } finally { setLoading(false) } }
  return <div className="modal-backdrop" onMouseDown={onClose}><section className="modal modal-form" role="dialog" aria-modal="true" onMouseDown={(e) => e.stopPropagation()}><button className="close-button" onClick={onClose}>×</button><p className="eyebrow">Sua conta</p><h2>{mode === 'login' ? 'Entre para continuar' : 'Faça parte da comunidade'}</h2><div className="auth-tabs"><button className={mode === 'login' ? 'active' : ''} onClick={() => setMode('login')}>Entrar</button><button className={mode === 'register' ? 'active' : ''} onClick={() => setMode('register')}>Criar conta</button></div><form onSubmit={submit}>{mode === 'register' && <><label>Nome<input required maxLength="80" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} /></label><label>Bairro<input required maxLength="40" value={form.neighborhood} onChange={(e) => setForm({ ...form, neighborhood: e.target.value })} /></label></>}<label>E-mail<input required type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} /></label><label>Senha<input required type="password" minLength="6" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} /></label>{error && <p className="form-error">{error}</p>}<button className="button button-full" disabled={loading}>{loading ? 'Aguarde…' : mode === 'login' ? 'Entrar' : 'Criar conta'}</button></form></section></div>
}

function DashboardModal({ currentUser, donations, interests, onClose, onOpenDonation, onDecision }) {
  const mine = donations.filter((item) => item.ownerId === currentUser.id); const received = interests.filter((item) => item.ownerId === currentUser.id); const sent = interests.filter((item) => item.interestedUserId === currentUser.id)
  return <div className="modal-backdrop" onMouseDown={onClose}><section className="modal dashboard" role="dialog" aria-modal="true" onMouseDown={(e) => e.stopPropagation()}><button className="close-button" onClick={onClose}>×</button><p className="eyebrow">Painel do usuário</p><h2>Olá, {currentUser.name.split(' ')[0]}</h2><h3>Minhas doações</h3>{mine.length ? <div className="dashboard-list">{mine.map((item) => <button key={item.id} onClick={() => onOpenDonation(item)}><span>{item.icon} {item.title}</span><strong>{statusLabels[item.status]}</strong></button>)}</div> : <p className="muted">Você ainda não publicou uma doação.</p>}<h3>Solicitações recebidas</h3>{received.length ? <div className="request-list">{received.map((item) => <article key={item.id}><div><strong>{item.interestedUserName}</strong><span> quer receber “{item.donationTitle}”</span></div><span className={`request-status status-${item.status}`}>{interestLabels[item.status]}</span>{item.status === 'pending' && <div><button className="accept-button" onClick={() => onDecision(item, 'accepted')}>Aceitar</button><button className="reject-button" onClick={() => onDecision(item, 'rejected')}>Recusar</button></div>}</article>)}</div> : <p className="muted">Nenhuma solicitação recebida.</p>}<h3>Meus interesses</h3>{sent.length ? <div className="request-list">{sent.map((item) => <article key={item.id}><span>{item.donationTitle}</span><span className={`request-status status-${item.status}`}>{interestLabels[item.status]}</span></article>)}</div> : <p className="muted">Você ainda não solicitou nenhum item.</p>}</section></div>
}

export default function App() {
  const [donations, setDonations] = useState(initialDonations); const [interests, setInterests] = useState([]); const [currentUser, setCurrentUser] = useState(null); const [queryText, setQueryText] = useState(''); const [category, setCategory] = useState('Todos'); const [selected, setSelected] = useState(null); const [formDonation, setFormDonation] = useState(); const [formOpen, setFormOpen] = useState(false); const [authOpen, setAuthOpen] = useState(false); const [dashboardOpen, setDashboardOpen] = useState(false); const [loading, setLoading] = useState(true); const [notice, setNotice] = useState('')
  useEffect(() => observeAuth(setCurrentUser), [])
  useEffect(() => { let active = true; listDonations(initialDonations).then((items) => active && setDonations(items)).catch(() => active && setNotice('Não foi possível carregar as doações.')).finally(() => active && setLoading(false)); return () => { active = false } }, [])
  useEffect(() => { if (currentUser) listInterests(currentUser).then(setInterests).catch(() => setNotice('Não foi possível carregar as solicitações.')); else setInterests([]) }, [currentUser])
  const filtered = useMemo(() => donations.filter((item) => (category === 'Todos' || item.category === category) && `${item.title} ${item.description} ${item.neighborhood}`.toLocaleLowerCase('pt-BR').includes(queryText.trim().toLocaleLowerCase('pt-BR'))), [donations, category, queryText])
  function requireAuth(action) { if (!currentUser) { setNotice('Entre ou crie uma conta para continuar.'); setAuthOpen(true); return false } action(); return true }
  async function saveDonation(form, existing) { try { const data = { ...form, icon: iconByCategory[form.category] }; const saved = existing ? await updateDonation(existing, data, currentUser) : await persistDonation(data, currentUser); setDonations((items) => existing ? items.map((item) => item.id === saved.id ? saved : item) : [saved, ...items]); setSelected(existing ? saved : null); setFormOpen(false); setNotice(existing ? 'Doação atualizada.' : 'Doação publicada com sucesso.') } catch (error) { setNotice(error.message) } }
  async function interest(donation) { if (!requireAuth(() => {})) return; try { const created = await createInterest(donation, currentUser); setInterests((items) => [created, ...items]); setSelected(null); setNotice('Interesse enviado ao doador.') } catch (error) { setNotice(error.message) } }
  async function status(donation, value) { try { const updated = await changeDonationStatus(donation, value, currentUser); setDonations((items) => items.map((item) => item.id === updated.id ? updated : item)); setSelected(updated); setNotice(`Item marcado como ${statusLabels[value].toLowerCase()}.`) } catch (error) { setNotice(error.message) } }
  async function remove(donation) { if (!window.confirm('Excluir esta publicação definitivamente?')) return; try { await deleteDonation(donation, currentUser); setDonations((items) => items.filter((item) => item.id !== donation.id)); setSelected(null); setNotice('Publicação excluída.') } catch (error) { setNotice(error.message) } }
  async function decision(item, value) { try { const updated = await decideInterest(item, value, currentUser); setInterests((items) => items.map((current) => current.id === updated.id ? updated : current)); if (value === 'accepted') setDonations((items) => items.map((donation) => donation.id === item.donationId ? { ...donation, status: 'reserved' } : donation)); setNotice(value === 'accepted' ? 'Solicitação aceita e item reservado.' : 'Solicitação recusada.') } catch (error) { setNotice(error.message) } }
  async function signOut() { await logout(); setDashboardOpen(false); setNotice('Sessão encerrada.') }
  return <><Header currentUser={currentUser} onAuth={() => setAuthOpen(true)} onDashboard={() => setDashboardOpen(true)} onDonate={() => requireAuth(() => { setFormDonation(undefined); setFormOpen(true) })} onLogout={signOut} /><main><div className={`mode-banner ${persistenceMode === 'firebase' ? 'connected' : ''}`}><strong>{persistenceMode === 'firebase' ? 'Firebase conectado' : 'Modo demonstração local'}</strong><span>{persistenceMode === 'firebase' ? 'Contas, doações e interesses são persistidos na nuvem.' : 'Os dados ficam somente neste navegador.'}</span></div><section className="hero" id="inicio"><div className="hero-copy"><p className="eyebrow">Solidariedade começa perto</p><h1>O que não serve mais para você pode transformar o dia de alguém.</h1><p>Doe itens em bom estado, encontre o que precisa e fortaleça sua comunidade.</p><div className="hero-actions"><a className="button" href="#doacoes">Encontrar doações</a><button className="button button-secondary" onClick={() => requireAuth(() => { setFormDonation(undefined); setFormOpen(true) })}>Quero doar</button></div><div className="trust-row"><span>✓ Gratuito</span><span>✓ Local</span><span>✓ Sem venda</span></div></div><div className="hero-art"><div className="sun"></div><div className="box"><span>♥</span></div><span className="floating-item item-one">📘</span><span className="floating-item item-two">🧸</span><span className="floating-item item-three">👕</span></div></section><section className="search-panel"><label className="search-field"><span>⌕</span><span className="sr-only">Pesquisar</span><input value={queryText} onChange={(e) => setQueryText(e.target.value)} placeholder="O que você está procurando?" /></label><label className="category-field"><span className="sr-only">Categoria</span><select value={category} onChange={(e) => setCategory(e.target.value)}>{categories.map((item) => <option key={item}>{item === 'Todos' ? 'Todas as categorias' : item}</option>)}</select></label></section>{notice && <div className="notice">{notice}<button onClick={() => setNotice('')}>×</button></div>}<section className="donations-section" id="doacoes"><div className="section-heading"><div><p className="eyebrow">Perto de você</p><h2>Doações</h2></div><p>{filtered.length} {filtered.length === 1 ? 'item encontrado' : 'itens encontrados'}</p></div>{loading ? <div className="empty-state"><h3>Carregando doações</h3></div> : filtered.length ? <div className="donation-grid">{filtered.map((item) => <DonationCard key={item.id} donation={item} onSelect={setSelected} />)}</div> : <div className="empty-state"><h3>Nenhuma doação encontrada</h3></div>}</section><section className="how-section" id="como-funciona"><div><p className="eyebrow">Simples do início ao fim</p><h2>Como funciona</h2></div><ol className="steps"><li><span>01</span><h3>Publique</h3><p>Cadastre um item.</p></li><li><span>02</span><h3>Conecte</h3><p>Alguém demonstra interesse.</p></li><li><span>03</span><h3>Combine</h3><p>Aprove e conclua a doação.</p></li></ol></section></main><footer><strong>AjudaVizinho</strong><span>Projeto acadêmico · Ciência da Computação</span></footer><DonationModal donation={selected} currentUser={currentUser} onClose={() => setSelected(null)} onEdit={(item) => { setSelected(null); setFormDonation(item); setFormOpen(true) }} onDelete={remove} onInterest={interest} onStatus={status} />{formOpen && <DonationFormModal donation={formDonation} onClose={() => setFormOpen(false)} onSave={saveDonation} />}{authOpen && <AuthModal onClose={() => setAuthOpen(false)} onAuthenticated={(user) => { setCurrentUser(user); setAuthOpen(false); setNotice(`Bem-vindo, ${user.name.split(' ')[0]}!`) }} />}{dashboardOpen && currentUser && <DashboardModal currentUser={currentUser} donations={donations} interests={interests} onClose={() => setDashboardOpen(false)} onOpenDonation={(item) => { setDashboardOpen(false); setSelected(item) }} onDecision={decision} />}</>
}
